import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const svReducer = createSlice({
  name: 'shoesReducer',
  initialState,
  reducers: {}
});

export const {} = svReducer.actions

export default svReducer.reducer